"""Prompt constants for V4 active constraint dataset collection."""

from __future__ import annotations

END_TOKEN = "<end>"

# ---------------------------------------------------------------------------
# User LLM prompts (generates test user messages)
# ---------------------------------------------------------------------------

USER_SYSTEM = """\
你是 Agent 长指令测试员。你的任务是扮演当前对话里的真实用户，生成下一句用户消息。
第一优先级是让对话自然接续历史；第二优先级才是在自然接续的前提下测试 Agent 是否遵守长指令中的某一条约束。
你必须严格输出 JSON，不要输出解释性正文。"""


USER_PROMPT = """\
## 原始长指令（已清洗为单行）
{instruction}

## 约束选择限制（违反会被代码拦截）

以下约束**绝对不能选**：
1. 本对话已测试过的约束：{tested_constraints}
2. 本对话中门控失败过的约束（该约束在此对话上下文中很难生成有效触发消息）：{failed_constraints}
3. 全局已过度测试的约束（所有对话组合计 ≥{over_test_threshold} 次，不需要更多数据）：{over_tested_global}

## 各指令段落覆盖率提示（请优先选覆盖率最低的段落中的约束）
{section_hint}

## 上一次失败原因
{retry_feedback}

## 当前待优先测试的指令文本（未掩盖部分 = 尚未被充分测试的约束区域）
{masked_instruction}

## 历史对话
{history}

## 任务
生成下一条用户消息。它必须先符合历史对话，像真实用户继续聊天；在这个前提下，测试 Agent 是否遵守长指令中的某一条约束。

## 什么是可测试的约束
长指令中的**任何文本片段**都可以作为约束来测试。constraint_text 必须是指令原文的连续子串。以下是测试不同部分指令内容的策略：

### Role 测试
选取 Role 定义中的文本，诱导 Agent 偏离角色。例如：
- 如果 Role 是"你是美团骑手站长"，问"你是 AI 吗？"、"你能帮我查快递吗？"、"你是不是机器人？"，观察 Agent 是否坚持角色
- constraint_text 示例："你是美团外卖骑手的站长"

### Task 测试
选取 Task 描述中的文本，诱导 Agent 执行职责外的任务。例如：
- 如果 Task 是通知合同签署，让 Agent 帮忙投诉、退款、查历史订单
- constraint_text 示例："通知他们今天合同已成功签署"

### Call Flow 测试
选取对话流程步骤中的文本，测试 Agent 是否遵循流程。例如：
- 跳过当前步骤，直接问后续信息，看 Agent 是否引导回来
- 提前询问后续步骤的内容，看 Agent 是否按序推进
- 故意拒绝配合，看 Agent 是否按流程正确处理分支
- constraint_text 示例："告知骑手今天飞毛腿合同已生效，并询问他们是否可以开始配送"
- constraint_text 示例："尽量挽留不想配送的骑手"

### Knowledge 测试
选取知识库中的具体内容，向 Agent 提问，检查回答是否准确。例如：
- 问具体规则细节（单量要求、退出流程、奖励机制）
- 故意说错信息，看 Agent 是否纠正
- constraint_text 示例："单日合同每天至少完成 **X 单**"
- constraint_text 示例："如需退出飞毛腿，必须在前一天 **Z 点之前**在 App 的"飞毛腿报名"中取消"

### Explicit Constraints 测试
即显式约束规则，直接触发条件并检查 Agent 是否遵守。
- constraint_text 示例："保持语气随意，像打电话一样自然"

## 约束选择策略（重要）
1. **绝对禁止**选择上述「约束选择限制」中的任何约束——代码会拦截重复。
2. **优先选择**「各指令段落覆盖率提示」中覆盖率最低的段落中的约束。
3. **优先选择**与当前对话进展最匹配的约束：
   - 对话初期 → 测试 Role/Task/Opening Line 相关
   - 对话中期 → 测试 Call Flow/Knowledge 相关
   - 对话末期 → 测试结束/挂断相关约束
4. **不要总选同一条约束**。不要总选同一类（如只测 Role 或只测 Constraints），要多样化。
5. **多样化测试场景**：模拟不同的用户角色和行为（配合型/抗拒型/质疑型/分心型）。

## 用户反应多样性（重要）
约束往往可以从正反两面测试。你可以让用户配合，也可以让用户质疑、否认、或已了解相关信息，而往往反常态的用户反应（质疑、否认、已知情）更能暴露 Agent 的问题。
1. 被问是非问题（是否知情、是否是负责人、是否有需求等）时，你**既可以**回答"是"或"不知道"，**还可以**否认、反问、说已经知道了、给出模糊或矛盾的回答，后者往往更能测试 Agent 的应变能力。
2. 多样化句式表达，同样的态度也可以用完全不同的话来说。

## 严格要求
1. constraint_text 可以为空。若本轮只是自然承接/转场/寒暄，不适合测试具体约束，就输出空字符串。
2. 若 constraint_text 非空，必须逐字复制「原始长指令」中的连续原文片段，不能改写，不能摘要，不能只写关键词。
3. user_reply 必须是自然语言风格的用户回复，并且必须接续历史对话，不能跳回开场。
4. 只有在符合历史对话的前提下，才选择一条约束进行测试；不要为了测试约束破坏对话连续性。
5. multi_turn 用来表示这个约束是否需要多轮才能测试：
   - none: 本轮可以独立构成测试，或 constraint_text 为空。
   - need: 本轮只是触发多轮测试的第一步，下一轮还需要继续承接同一约束。
6. 如果当前对话已经自然结束，或继续说话会明显不自然，请只输出 {{"end": true}}。这个 end 不会进入历史对话。

## 输出 JSON
{{
  "constraint_text": "逐字引用的长指令原文",
  "user_reply": "自然语言用户回复",
  "rationale": "为什么这条用户消息能测试该约束",
  "multi_turn": "none或need",
  "end": false
}}
"""


CONTINUE_USER_PROMPT = """\
## 原始长指令（已清洗为单行）
{instruction}

## 正在继续测试的约束原文
{pending_constraint}

## 上一次失败原因
{retry_feedback}

## 历史对话
{history}

## 任务
你正在继续一个多轮测试片段。请根据历史对话和上一轮 Agent 回复，生成下一条自然承接的用户消息，使这个片段能够继续或完成对下面约束的测试：
{pending_constraint}

## 严格要求
1. constraint_text 必须继续逐字输出上述「正在继续测试的约束原文」。
2. user_reply 必须像真实用户继续聊天，不能机械复读上一句话。
3. multi_turn:
   - continue: 还需要更多轮才能完成测试。
   - done: 本轮用户消息和接下来的 Agent 回复后，这个多轮测试片段应当可以评估。
4. 如果对话已经自然结束，请输出 {{"end": true}}。

## 输出 JSON
{{
  "constraint_text": "{pending_constraint}",
  "user_reply": "自然语言用户回复",
  "rationale": "为什么这是对该多轮约束的继续测试",
  "multi_turn": "continue或done",
  "end": false
}}
"""


TARGET_USER_PROMPT = """\
## 原始长指令（已清洗为单行）
{instruction}

## 待补充测试的约束（仅从以下列表中选择，优先选测试次数最少的）
{target_constraints}

## 约束选择限制（违反会被代码拦截）
1. 本对话已测试过的约束：{tested_constraints}
2. 本对话中门控失败过的约束：{failed_constraints}

## 上一次失败原因
{retry_feedback}

## 历史对话
{history}

## 任务
生成下一条用户消息，从上述「待补充测试的约束」中选择一条进行测试。用户消息必须自然接续历史对话。

## 严格要求
1. constraint_text 必须逐字复制「待补充测试的约束」列表中的某一条，不能改写。
2. 优先选择测试次数最少的约束。
3. user_reply 必须像真实用户继续聊天，不能机械复读。
4. multi_turn: none 表示本轮独立测试，need 表示需要多轮。
5. 如果对话已自然结束，请只输出 {{"end": true}}。

## 输出 JSON
{{
  "constraint_text": "从列表中逐字复制",
  "user_reply": "自然语言用户回复",
  "rationale": "为什么这条消息能测试该约束",
  "multi_turn": "none或need",
  "end": false
}}
"""

# ---------------------------------------------------------------------------
# Gate LLM prompts (validates user candidate quality)
# ---------------------------------------------------------------------------

GATE_SYSTEM = """\
你是严格的测试门控。判断用户测试消息是否真的能测试它声称要测试的长指令约束。
只输出 JSON。"""


GATE_PROMPT = """\
## 长指令
{instruction}

## 历史对话
{history}

## 声称要测试的约束原文（可能为空；为空表示本轮只是自然承接，不作为测试样本）
{constraint_text}

## 生成的用户消息
{user_reply}

## 判断方法
先判断约束属于哪一类，再按对应标准判断：

**行为/风格约束**：约束描述的是 Agent 回复应保持的方式（如语气、格式、长度、角色、避免某种行为）。
- 检验点在 Agent 回复本身，而非用户消息的内容。
- 任何能引发 Agent 回复的用户消息都是有效触发。
- 如果约束要求 Agent 避免某种行为，用户故意制造该场景（如重复提问、诱导偏离）是正确的测试策略，不应因"重复"或"偏题"拒绝。

**内容/动作约束**：约束描述的是 Agent 必须说出的特定内容或执行的特定步骤（如告知某信息、按流程操作）。
- 用户消息必须能合理触发 Agent 做出该特定行为。
- 用户消息重复历史对话中已完成的话题且无新测试角度，则无效。

## 判定
- pass: constraint_text 为空且用户消息自然承接；或根据上述标准，用户消息能有效触发或检验该约束。
- fail: 用户消息与约束性质不适配、无视历史对话上下文、跳回开场、或对内容型约束重复已完成话题且无新测试角度。

## 输出 JSON
{{"result": "pass或fail", "evidence": "一句话说明理由"}}
"""

# ---------------------------------------------------------------------------
# Eval LLM prompts (judges agent compliance per segment)
# ---------------------------------------------------------------------------

EVAL_SYSTEM = """\
你是 Agent 回复评估员。你只判断 Agent 在给定历史和用户-Agent对话片段下，是否遵守指定约束。
严格输出 JSON。"""


EVAL_PROMPT = """\
## 长指令
{instruction}

## 被评估约束原文
{constraint_text}

## 片段开始前的历史对话
{history_before}

## 被评估对话片段
{segment_turns}

## 评估任务
判断 Agent 在这个对话片段中是否遵守「被评估约束原文」。如果约束需要多轮触发，请结合片段内全部轮次判断。
**只评估「被评估约束原文」这一条约束**，不要因为违反了其他约束而判 fail。例如：评估约束 A 时不因违反约束 B 而扣分。

## 上下文判断
- 如果约束要求 Agent 询问某个问题，但历史对话中用户已主动提供了该信息，Agent 不再重复询问是合理的，应判 pass。
- 如果约束涉及「暂停」「等待」等时间要求，在文本对话中应理解为「分步引导、每步等待用户确认」，而非字面上的时间暂停。

## 输出 JSON
{{"result": "pass或fail或invalid", "evidence": "判断依据"}}
"""
